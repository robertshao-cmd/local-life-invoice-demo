# ⚠️ 這是靜態快照，不是正式版

這個資料夾是某次手動複製過來的 UI 快照，**沒有任何機制讓它跟著正式部署更新**。
每次正式版推上去，這裡就更舊一點。

## 正式版在哪

| 單元 | 正式網址 | 狀態 |
|---|---|---|
| 無聊快篩 bored | <https://ailab-dev.cmoney.tw/bored/> | ✅ 已上線 |
| 幾歲破產 wealth | <https://ailab-dev.cmoney.tw/wealth/> | ✅ 已上線 |
| 解毒吧 detox | — | ❌ **尚未上線**（`/detox/` 會 302 轉去不相干的地方；`/check-food-safety` 只有落地頁） |

正式部署路徑：`miniapps/<單元>/` → GitLab (`xlab/ailab/10_million_project_group/<單元>`) → CI → Cloud Run。
**本資料夾不在那條路徑上。**

## 落差實測（2026-08-16）

| 單元 | 原始碼 | 本資料夾 | 差 |
|---|---|---|---|
| wealth | 122,596 bytes | 103,895 | **少 18,701**（缺 cid 個人化、GA4 埋點、cid 徽章那批） |
| bored | 105,922 | 103,801 | 少 2,121（缺 homeHint 死連結修正） |
| detox | 57,345 | 56,612 | 少 733 |

## 那還留著它幹嘛

1. **detox 還沒上線**——在它上線前，這是唯一能看到 UI 的地方。
2. **小程序架（殼）** 展示三顆並排的貨架長相，正式站沒有這個組合視圖。
3. 不能連內網時的備援。

## 它做不到什麼

純靜態、**沒有後端**：wealth 的 cid 個人化查不到本人發票、detox 的分類 API 不會回應、
GA4 埋點不會送出。只能看版面，不能驗行為。

## 誰該更新它

沒有人自動更新。如果要讓它跟上，手動從 `miniapps/<單元>/<單元>/` 複製，
或替 `scripts/publish-demo.py` 加一段同步邏輯。
但先想清楚：**已上線的單元直接連正式站更好**，這裡只該留還沒上線的東西。
